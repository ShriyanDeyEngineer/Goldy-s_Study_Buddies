/** Skeleton for friends & buddies (five list queries plus a profile
 *  lookup). */
import { PageSkeleton } from "@/components/ui/skeleton";

export default function Loading() {
  return <PageSkeleton cards={3} />;
}

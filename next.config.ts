/**
 * Next.js configuration.
 *
 * There is intentionally nothing here. The app ships no remote images
 * (avatars are initials-only — migration 0037) and every Server Action
 * payload is small text, so the framework defaults are correct. Touch
 * this file only if you add a redirect, a header, or a genuine need for
 * a raised body limit / a remote image host.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
